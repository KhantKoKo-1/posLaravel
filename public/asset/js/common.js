const base_url = 'http://localhost:8000/';
const shift_id = document.getElementById('shift_id').value;

function printPage() {
    window.print();
}

function backPage() {
    window.history.back();
}
