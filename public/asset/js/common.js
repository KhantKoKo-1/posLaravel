const base_url = 'http://127.0.0.1:8090/';
const shift_id = document.getElementById('shift_id').value;

function printPage() {
    window.print();
}

function backPage() {
    window.history.back();
}